version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    # Add any necessary environment variables here or use env_file
    # env_file:
    #   - .env
